name: Tests

on:
  push:
  pull_request:
  schedule:
    - cron:  '0 0 * * 0'

jobs:
  standard-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build the project
        run: |
          echo "UID=$(id --user)" > .env
          docker-compose up -d --build
      - name: Lint the code
        run: |
          docker-compose exec -T document-merge-service black --check .
          docker-compose exec -T document-merge-service flake8
          docker-compose exec -T document-merge-service ./manage.py makemigrations --check --dry-run --no-input
          docker-compose exec -T document-merge-service reuse lint
      - name: Run pytest
        run: docker-compose exec -T document-merge-service pytest --no-cov-on-fail --cov --create-db -vv
  gitlint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: Setup Python
        uses: actions/setup-python@v1
        with:
          python-version: '3.7'
      - name: Install gitlint
        run: pip install gitlint
      - name: Run gitlint
        run: gitlint --contrib contrib-title-conventional-commits
